# -*- mode: snippet -*-
# name: flake-sys
# key: flksys
# --
{
  inputs.nixpkgs.url = "sys";
  outputs = { self, nixpkgs }:
    let
      system = "x86_64-linux";
      pkgs = nixpkgs.legacyPackages.\${system};
    in {
      devShell.\${system} = with pkgs; mkShell {

      };
    };
}
