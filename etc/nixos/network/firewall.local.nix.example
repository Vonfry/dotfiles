{ config, pkgs, ... }:

{
  networking.firewall = {
      enable = true;
      allowedTCPPorts = [ 22000 22 ];
      allowedTCPPortRanges = [];
      allowedUDPPorts = [ 21027 ];
      allowPing = false;
  };
}
