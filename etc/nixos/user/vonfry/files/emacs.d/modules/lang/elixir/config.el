;;; elixir config -*- lexical-binding: t -*-
;;

;; +mmap-mode-elixir-def
(mmap-mode-prefix! elixir nil
  :keymaps '(elixir-mode-map))

;; +mmap-cc-def
(mmap-leader-prefix! elixir nil
  :keymaps '(elixir-mode-map))
