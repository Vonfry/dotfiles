set editor="nvim"

source ~/.mutt/vonfry/alias

set header_cache = ~/.cache/mutt_header
set message_cachedir = ~/.cache/mutt

set edit_headers = yes
set charset = UTF-8
unset use_domain
set use_from = yes
