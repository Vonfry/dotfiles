;;; treemacs config -*- lexical-binding: t -*-
;;


(defcustom +treemacs-persist-file
  (expand-file-name "treemacs-persist" vonfry-cache-dir)
  "treemacs persist-file, see `treemacs-persist-file'"
  :type 'file
  :group 'vonfry-modules)
